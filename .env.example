# Copie este arquivo para `.env` e preencha os valores.
# (Nunca commite o `.env` real.)

# Supabase (Vite client)
# Variáveis expostas ao frontend precisam do prefixo VITE_
VITE_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
VITE_SUPABASE_ANON_KEY=YOUR_SUPABASE_ANON_KEY

# Supabase (server)
# Necessário para o backend fazer operações admin (server/supabase.ts)
SUPABASE_SERVICE_ROLE_KEY=YOUR_SUPABASE_SERVICE_ROLE_KEY

# API (Express)
# Opcional: por padrão o servidor sobe na 8787
# API_PORT=8787

# n8n - Marketplace (checkout)
# Endpoint do seu workflow no n8n que cria a sessão de checkout
N8N_MARKETPLACE_CHECKOUT_URL=http://localhost:5678/webhook/your-marketplace-checkout
# Opcional: se seu webhook exigir Authorization: Bearer <token>
N8N_MARKETPLACE_CHECKOUT_SECRET=

# Opcional (timeouts/política)
# N8N_MARKETPLACE_CHECKOUT_TIMEOUT_MS=15000
# STRIPE_CHECKOUT_EXPIRES_MINUTES=60

# n8n -> app (autoriza /api/n8n/purchases/update)
# Use um segredo forte e igual no n8n
N8N_MARKETPLACE_PURCHASE_UPDATE_SECRET=CHANGE_ME

# n8n - Creations (geração)
# Endpoint do workflow no n8n que recebe prompts e gera as músicas
N8N_CREATIONS_WEBHOOK_URL=http://localhost:5678/webhook/your-creations
# Opcional: se seu webhook exigir Authorization: Bearer <token>
N8N_CREATIONS_WEBHOOK_SECRET=

# Opcional: timeout (ms)
# N8N_CREATIONS_WEBHOOK_TIMEOUT_MS=15000
